# Copy to .env if you use a dotenv loader, or export manually in your shell.
# Required for make eboot / make pkg / scripts/build.sh

# OpenOrbis PS4 Toolchain root (contains bin/, include/, lib/, samples/)
OO_PS4_TOOLCHAIN=/path/to/OpenOrbis-PS4-Toolchain

# Optional overrides
# DEBUG=0
# HOST_CXX=clang++
